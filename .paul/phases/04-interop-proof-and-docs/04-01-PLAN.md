---
phase: 04-interop-proof-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/hashline-interop-smoke.test.ts
  - README.md
  - docs/hashline-integration/START-HERE.md
  - docs/hashline-integration/ROADMAP.md
autonomous: true
---

<objective>
## Goal
Deliver Phase 4 proof that `pi-ptc-next` and hashline-style overrides work together in one lightweight combined workflow, and finalize milestone-facing interop documentation around the `details.ptcValue` contract.

## Purpose
Phases 1–3 established runtime seam correctness, structured-result precedence, and documentation/guidance reconciliation. This final milestone phase closes the confidence gap with a focused together-smoke proof and concise docs so maintainers can trust and reproduce interop behavior.

## Output
- A lightweight smoke regression test that exercises a combined interop path with hashline-style override behavior and verifies structured value flow in `code_execution`
- Documentation updates that describe recommended interop setup/expectations and point to the smoke-proof path
- Green targeted + full-suite verification confirming no regressions
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/03-verification/03-01-SUMMARY.md
README.md
docs/hashline-integration/START-HERE.md
docs/hashline-integration/ROADMAP.md

## Prior Work (only if genuinely needed)
.paul/phases/02-result-normalization/02-01-SUMMARY.md
.paul/phases/03-verification/03-01-SUMMARY.md

## Source Files
test/hashline-interop-smoke.test.ts
README.md
docs/hashline-integration/START-HERE.md
docs/hashline-integration/ROADMAP.md
</context>

<acceptance_criteria>
## AC-1: Lightweight interop smoke proof exists and is runnable
```gherkin
Given `pi-ptc-next` is running with hashline-style override behavior in a combined workflow
When the new smoke regression test executes
Then it proves `code_execution` receives structured machine-native values in the interop path without breaking existing behavior
```

## AC-2: Interop docs describe the final contract and how to verify it
```gherkin
Given a maintainer reads the interop docs
When they review milestone guidance
Then they can see the recommended `details.ptcValue` contract expectations, smoke-proof location, and the intended lightweight setup approach
```

## AC-3: Milestone baseline remains stable
```gherkin
Given the smoke proof and docs updates are in place
When targeted and full-suite verification run
Then all relevant tests pass and no prior milestone behavior regresses
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add a lightweight combined interop smoke regression test</name>
  <files>test/hashline-interop-smoke.test.ts</files>
  <action>
    Implement a focused test that exercises a realistic combined interop path for `code_execution` with hashline-style override semantics and validates structured value flow in the result path.
    Keep the harness lightweight and deterministic (no heavy external orchestration), but ensure it captures the key together-proof behavior this phase is responsible for.
    Reuse existing repo testing patterns where possible so maintenance burden stays low.
    Avoid: broad harness architecture, large end-to-end environment scaffolding, or refactoring core runtime components outside smoke-proof needs.
  </action>
  <verify>npm run build && node --test test/hashline-interop-smoke.test.ts</verify>
  <done>AC-1 satisfied: the repo has a runnable lightweight smoke proof for combined interop behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize milestone interop documentation and smoke-proof references</name>
  <files>README.md, docs/hashline-integration/START-HERE.md, docs/hashline-integration/ROADMAP.md</files>
  <action>
    Update docs to clearly communicate the final Milestone 2 interop contract: active override execution, `details.ptcValue` machine-native passthrough expectations, and where the smoke proof lives.
    Keep documentation concise and operational: what is proven, how to run proof, and what remains explicitly deferred beyond this milestone.
    Ensure wording aligns with current Phase 4 scope and does not over-promise Milestone 3 ergonomics work.
    Avoid: broad doc rewrites, speculative future APIs, or introducing guidance that conflicts with current runtime/test reality.
  </action>
  <verify>grep -n "ptcValue\|smoke\|hashline\|interop" README.md docs/hashline-integration/START-HERE.md docs/hashline-integration/ROADMAP.md</verify>
  <done>AC-2 satisfied: maintainers have clear interop contract and smoke-proof navigation in docs.</done>
</task>

<task type="auto">
  <name>Task 3: Run milestone-close verification and capture stability evidence</name>
  <files>test/hashline-interop-smoke.test.ts, README.md, docs/hashline-integration/START-HERE.md, docs/hashline-integration/ROADMAP.md</files>
  <action>
    Execute targeted smoke-test verification and then the full repository test suite.
    Confirm the new Phase 4 artifacts coexist with previously landed Phase 1–3 behavior, including adapter/rpc/index coverage.
    Record any notable warnings or constraints for UNIFY so phase closure is explicit and auditable.
    Avoid: changing scope mid-verify; if a blocker appears, document it as a deviation rather than silently expanding implementation.
  </action>
  <verify>npm run build && node --test test/hashline-interop-smoke.test.ts && npm test</verify>
  <done>AC-3 satisfied: targeted + full-suite checks pass with no regression from the new Phase 4 proof/docs work.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/python-runtime/*` and deep RPC framing internals unrelated to smoke-proof requirements
- `src/tool-adapters.ts`, `src/rpc-protocol.ts`, and their prior-phase tests unless a focused failing smoke proof requires a minimal corrective touch
- Milestone 3 ergonomics/metadata scope (`src/tools/python-tool-contract.ts` redesigns, helper-type expansion)
- Dependency/tooling footprint (no heavy new harness frameworks)

## SCOPE LIMITS
- This plan is strictly Phase 4 interop proof + milestone docs closure
- Keep smoke proof lightweight and maintainable; do not build a heavyweight end-to-end platform
- No broad hashline-side IR refactor work and no unrelated runtime architecture changes
- Do not perform git automation/branching/tagging changes in this plan
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `node --test test/hashline-interop-smoke.test.ts` passes
- [ ] `npm test` passes with no regressions
- [ ] Docs mention interop smoke proof location and `details.ptcValue` contract expectations
- [ ] Phase 4 scope remains lightweight (no unintended harness/framework expansion)
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- Repo contains a clear, runnable lightweight together-smoke proof for interop
- Milestone docs make contract + proof discoverable and reproducible
- Full suite remains green with Phase 4 changes
</success_criteria>

<output>
After completion, create `.paul/phases/04-interop-proof-and-docs/04-01-SUMMARY.md`
</output>
