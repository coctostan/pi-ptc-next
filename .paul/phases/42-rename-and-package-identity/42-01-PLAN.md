---
phase: 42-rename-and-package-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/verify-release-package.sh
autonomous: true
---

<objective>
## Goal
Rename the published package identity from `@cegersdo/pi-ptc` / `pi-ptc-next` packaging metadata to the new publish target `pi-ptc-advanced`, set the next release version baseline to `0.15.0`, and align the release-package verification path with that new package identity.

## Purpose
This phase establishes a publishable package surface without mixing in the broader README/release-documentation rewrite. It advances the project from an internally maintained fork toward a real distributable package while keeping the package-identity work small, testable, and reversible.

## Output
Updated package metadata and lock metadata for `pi-ptc-advanced` at `0.15.0`, plus a release-package verification script that validates the new baseline.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/41-behavior-consistency-and-follow-up-proof/41-01-SUMMARY.md

## Source Files
package.json
package-lock.json
scripts/verify-release-package.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → test infrastructure detected (`node --test` via `npm test`); IRIS(150) → no actionable anti-pattern findings in planned package/release-script files; DAVE(200) → `.github/workflows/ci.yml` present, repo already has CI coverage; DOCS(200) → documentation drift risk expected because package identity and version strings currently appear in README + docs/release runbooks and are intentionally deferred to Phase 43; RUBY(250) → planned files are small metadata/shell surfaces with no debt hotspot concerns.

[dispatch] pre-plan enforcement: DEAN(50) → `npm audit --json` reports 0 critical / 2 high / 3 moderate / 1 low; no critical block for plan creation, but existing high/moderate advisory baseline remains known context for publish-readiness follow-up.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured (`.paul/SPECIAL-FLOWS.md` not present).
</skills>

<acceptance_criteria>
## AC-1: Package identity baseline is renamed and versioned
```gherkin
Given the repository still identifies the package as `@cegersdo/pi-ptc` at `0.8.0`
When Phase 42 package metadata work is complete
Then the publish surface names the package `pi-ptc-advanced` and sets the intended release baseline to `0.15.0` in package metadata files used by npm packaging
```

## AC-2: Release-package verification matches the new publish target
```gherkin
Given the repo uses `scripts/verify-release-package.sh` to validate package metadata and tarball shape
When the package rename/version update lands
Then the verification script checks the new `pi-ptc-advanced` / `0.15.0` baseline instead of the old `0.8.0` fork baseline
```

## AC-3: Package-identity work stays scoped to Phase 42
```gherkin
Given README and release-facing docs still contain old fork naming and baseline references
When Phase 42 is finished
Then package metadata and package-surface verification are aligned for the new identity, while broader README/history/credit updates remain explicitly deferred to Phase 43
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Rename package metadata to the new publish target</name>
  <files>package.json, package-lock.json</files>
  <action>
    Update npm package identity to the new publish target `pi-ptc-advanced` and the next release baseline `0.15.0`.
    - Change the package name in `package.json`.
    - Update the package version in `package.json`.
    - Align package-lock root metadata (`name` and `version`) with the same values so `npm pack`/install flows reflect the new baseline consistently.
    - Preserve existing repository/homepage/bugs linkage unless Phase 42 work proves those URLs must move as part of package identity.
    Avoid: mixing broad README/doc rewrites into this task; those belong to Phase 43.
  </action>
  <verify>grep -n '"name"\|"version"' package.json package-lock.json | head -20</verify>
  <done>AC-1 satisfied: package metadata files expose `pi-ptc-advanced` and `0.15.0` consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Align release-package verification with the new identity baseline</name>
  <files>scripts/verify-release-package.sh</files>
  <action>
    Update the release-package verification script so it validates the renamed package surface.
    - Replace the old hard-coded `0.8.0` expectation with the new `0.15.0` baseline.
    - Add or tighten checks that meaningfully confirm the package metadata matches the new publish target if the current script does not already prove that.
    - Keep the tarball-entry validation behavior intact unless package rename work requires a minimal adjustment.
    Avoid: adding publish automation, git tagging logic, or broader release orchestration in this phase.
  </action>
  <verify>bash scripts/verify-release-package.sh</verify>
  <done>AC-2 satisfied: release-package verification proves the renamed/versioned package surface.</done>
</task>

<task type="auto">
  <name>Task 3: Revalidate the package surface without opening the docs phase</name>
  <files>package.json, package-lock.json, scripts/verify-release-package.sh</files>
  <action>
    Run focused verification on the Phase 42 package-identity slice and capture any remaining references that should stay deferred to Phase 43.
    - Run the package-surface verification path.
    - Run at least a build-level check so metadata/script edits do not leave the repo in a broken release state.
    - Record any remaining README/docs/release-note naming drift as intentional follow-up context for Phase 43 rather than silently fixing it here.
    Avoid: opportunistically editing README or release docs during this task unless the package verification path cannot run without a minimal emergency correction.
  </action>
  <verify>npm run build && bash scripts/verify-release-package.sh && grep -Rni 'pi-ptc-next\|@cegersdo/pi-ptc\|0.8.0' README.md docs 2>/dev/null | head -40</verify>
  <done>AC-3 satisfied: package identity slice is verified and remaining docs drift is explicitly preserved for the next phase.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- README.md content/body and install guidance beyond what is strictly required for package verification failure triage
- `docs/releases/*`, `docs/personal-fork-maintenance.md`, and other release-facing docs (these belong to Phase 43)
- Source/runtime behavior files under `src/**`
- Test suites unrelated to package-surface verification unless Phase 42 exposes a direct failing assertion tied to package identity

## SCOPE LIMITS
- This plan covers package identity and package-surface verification only
- Do not add publish automation, registry publishing commands, or release-tag workflow changes in this phase
- Do not attempt to resolve existing dependency-audit findings in this phase unless they directly block package verification
- Keep fork-history acknowledgment work deferred to Phase 43
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] `bash scripts/verify-release-package.sh`
- [ ] `grep -n '"name"\|"version"' package.json package-lock.json | head -20`
- [ ] Remaining README/docs naming drift captured as intentional Phase 43 follow-up
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- Package metadata exposes `pi-ptc-advanced` at `0.15.0`
- Release-package verification passes against the new baseline
- No source/runtime behavior changes introduced
- README/history/credit work remains clearly deferred to Phase 43
</success_criteria>

<output>
After completion, create `.paul/phases/42-rename-and-package-identity/42-01-SUMMARY.md`
</output>
