---
phase: 60-1-0-release-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/release-readiness.test.ts
  - docs/releases/PUBLISH-CHECKLIST.md
  - docs/releases/1.0.0.md
  - CHANGELOG.md
autonomous: true
---

<objective>
## Goal
Close the `pi-ptc-advanced@1.0.0` release gate by adding a durable, repo-owned **publish dry-run checklist + manual publish instructions** and the release-readiness drift guards that keep it accurate — without performing any actual `npm publish`, git tag, GitHub release, or repo rename.

## Purpose
Milestone 20 promises a clean, public 1.0 release of `pi-ptc-advanced`. Phases 58–59 set the package identity, README, and docs baseline. Phase 60 is the gate: the repo must give a user (or future maintainer) a deterministic, verifiable path from green CI to `npm publish` without ambiguity, while keeping publish/tag/rename explicitly user-owned. This phase finishes the release-readiness contract that the rest of the milestone (Phase 61 rename/migration proof) will build on.

## Output
- Extended `test/release-readiness.test.ts` with drift guards for the new checklist doc and refreshed 1.0.0 manual-boundary cross-link.
- New `docs/releases/PUBLISH-CHECKLIST.md`: green-CI preconditions, `npm pack --dry-run` + `npm publish --dry-run` steps, manual publish instructions, post-publish manual steps (tag / GitHub release / repo rename pointer), and explicit "stop here for automated APPLY" line.
- Refreshed `docs/releases/1.0.0.md` "Manual boundaries" section linking to the checklist.
- Refreshed `CHANGELOG.md` 1.0.0 entry referencing the checklist as part of the release-gate surface.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
- `.paul/phases/58-public-identity-rename/58-01-SUMMARY.md` — established `pi-ptc-advanced@1.0.0` package/release baseline.
- `.paul/phases/59-readme-and-docs-polish/59-01-SUMMARY.md` — public README/runbook framing + Phase 59 drift guards in `test/release-readiness.test.ts` (direct input: this plan extends the same drift-test file).

## Source Files
test/release-readiness.test.ts
docs/releases/1.0.0.md
CHANGELOG.md
scripts/verify-release-package.sh
package.json
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: 6 modules dispatched
- TODD: `tdd_candidates` → plan type = `tdd`; extend `test/release-readiness.test.ts` (RED) before adding `docs/releases/PUBLISH-CHECKLIST.md` (GREEN).
- IRIS: `review_flags` → none (docs/test-only slice; no runtime surface change).
- DAVE: `deploy_warning` → explicit manual boundary; APPLY must NOT run `npm publish`, `git tag`, or `gh release create`. Only `npm publish --dry-run` is allowed, and only as captured evidence inside the checklist doc (not as an APPLY action).
- DOCS: `doc_warnings` → high relevance; checklist doc, `CHANGELOG.md` 1.0.0 entry, and `docs/releases/1.0.0.md` manual-boundary section must stay in sync; drift tests enforce this.
- CODI: `blast_radius` → 0 seeds (no `src/**` TS/JS identifiers in scope); skipped cleanly.
- RUBY: `debt_flags` → none for the touched files.

[dispatch] pre-plan enforcement: 1 module
- DEAN: `npm audit` = `0 critical / 0 high / 3 moderate / 0 low` — matches `.paul/dean-baseline.json` (acknowledged through 2026-06-11). PASS, no block.
</module_dispatch>

<acceptance_criteria>

## AC-1: Publish checklist drift guard
```gherkin
Given the release-readiness test suite at test/release-readiness.test.ts
When `node --test test/release-readiness.test.ts` runs
Then it asserts that docs/releases/PUBLISH-CHECKLIST.md exists,
 And contains required headings ("Preconditions", "Dry-run", "Manual publish", "Post-publish manual steps", "Stop here for automated APPLY"),
 And mentions both `npm pack --dry-run` and `npm publish --dry-run`,
 And references `pi-ptc-advanced@1.0.0` exactly,
 And contains no premature claims that publish/tag/release/rename have happened.
```

## AC-2: Release-doc cross-link drift guard
```gherkin
Given the release-readiness test suite
When the suite runs
Then it asserts that docs/releases/1.0.0.md's "Manual boundaries" section links to docs/releases/PUBLISH-CHECKLIST.md,
 And asserts that CHANGELOG.md's 1.0.0 entry mentions the checklist as part of the release-gate surface.
```

## AC-3: Manual-boundary contract preserved
```gherkin
Given Phase 60 is a release-gate phase
When APPLY runs
Then no `npm publish`, `git tag`, `gh release create`, or repo-rename command is executed,
 And the new checklist doc explicitly instructs the user that those steps are manual and user-owned,
 And full verification passes: `npm test`, `npm run build`, `bash scripts/verify-release-package.sh`, and `npm run verify:ci` all green.
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Extend release-readiness drift guards for checklist + cross-links</name>
  <files>test/release-readiness.test.ts</files>
  <action>
    Add a new `describe`/test block "publish checklist and 1.0 release-gate cross-links" with bounded assertions, written BEFORE the doc exists so the tests RED-fail meaningfully:
    - Read `docs/releases/PUBLISH-CHECKLIST.md` and assert it exists.
    - Assert presence of headings (case-insensitive substring match against `^## ` lines): "Preconditions", "Dry-run", "Manual publish", "Post-publish manual steps", "Stop here for automated APPLY".
    - Assert it contains the strings `npm pack --dry-run` and `npm publish --dry-run`.
    - Assert it contains `pi-ptc-advanced@1.0.0` literally.
    - Assert it does NOT contain phrases that imply publish has already happened (negative substring checks for: "has been published", "is published on npm", "tag created", "release created", "repo renamed").
    - Read `docs/releases/1.0.0.md` and assert its "Manual boundaries" section text contains a relative link to `PUBLISH-CHECKLIST.md`.
    - Read `CHANGELOG.md`, locate the `## 1.0.0` heading, and assert the entry between that heading and the next `## ` heading mentions `PUBLISH-CHECKLIST.md` (or "publish checklist" as a substring).
    - Reuse the existing test file's import/style conventions (`node:test`, `node:assert/strict`, `node:fs/promises`, `path.resolve(process.cwd(), ...)`); do not introduce new test frameworks.
    Avoid: editing unrelated existing assertions, weakening Phase 59 drift guards, or asserting on `dist/**` output.
  </action>
  <verify>node --test test/release-readiness.test.ts  # MUST fail on the new block before Task 2 (RED proof)</verify>
  <done>AC-1, AC-2 wired as failing assertions before any doc work.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Add publish checklist doc and refresh release-gate cross-links</name>
  <files>docs/releases/PUBLISH-CHECKLIST.md, docs/releases/1.0.0.md, CHANGELOG.md</files>
  <action>
    Create `docs/releases/PUBLISH-CHECKLIST.md` as the durable release-gate checklist for `pi-ptc-advanced@1.0.0`. Required sections (each as `## ` headings, in order):
      1. `## Preconditions` — clean `main`, green CI, audit baseline (`0c/0h/3m/0l`) acknowledged, `package.json` version = `1.0.0`, working tree clean.
      2. `## Dry-run` — exact commands in order: `npm test`, `npm run build`, `bash scripts/verify-release-package.sh`, `npm run verify:ci`, `npm pack --dry-run`, `npm publish --dry-run`. Note that `npm publish --dry-run` requires npm login but does not transmit; document the expected tarball name `pi-ptc-advanced-1.0.0.tgz`.
      3. `## Manual publish` — explicit user-owned steps: `npm login`, `npm publish --access public` (because the package is unscoped/public), capture the published version with `npm view pi-ptc-advanced version`. State clearly that automated APPLY does not run these.
      4. `## Post-publish manual steps` — `git tag v1.0.0 && git push origin v1.0.0`, GitHub release creation pointer (`gh release create v1.0.0 --notes-file docs/releases/1.0.0.md`), and a forward pointer to Phase 61 for GitHub repo rename to `coctostan/pi-ptc-advanced`.
      5. `## Stop here for automated APPLY` — single short paragraph stating that PALS automated APPLY stops before `npm publish` and the steps above are manual/user-owned.
    Reference `pi-ptc-advanced@1.0.0` literally at least once. Do NOT claim publish, tag, release, or rename have occurred.

    Update `docs/releases/1.0.0.md` "Manual boundaries" section: add a sentence linking to `./PUBLISH-CHECKLIST.md` ("See [PUBLISH-CHECKLIST.md](./PUBLISH-CHECKLIST.md) for the dry-run + manual publish sequence.") without removing the existing manual-boundary text.

    Update `CHANGELOG.md` 1.0.0 entry: add a bullet under "Changed" (or a new "Release gate" subsection above "Deferred / not included") referencing `docs/releases/PUBLISH-CHECKLIST.md` as the repo-owned publish checklist; keep "Deferred / not included" intact.

    Avoid: editing the "Unreleased" section to claim work that isn't shipped, modifying older release notes, or introducing an `npm publish` invocation anywhere.
  </action>
  <verify>node --test test/release-readiness.test.ts  # all green, including the new block</verify>
  <done>AC-1 and AC-2 satisfied; doc + cross-links present and consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Full release-gate verification (dry-run only)</name>
  <files>(verification only — no source edits)</files>
  <action>
    Run, in order, capturing output:
    1. `npm test` — full suite must pass.
    2. `npm run build` — clean build.
    3. `bash scripts/verify-release-package.sh` — package + tarball + clean-install proof.
    4. `npm run verify:ci` — repo-owned CI parity.
    5. `npm pack --dry-run` — confirm tarball surface unchanged from Phase 58/59 baseline (expected name: `pi-ptc-advanced-1.0.0.tgz`).
    Do NOT run `npm publish`, `npm publish --dry-run` against a real registry session, `git tag`, or `gh release create` as part of APPLY — those remain manual per the checklist doc and AC-3.
    If `npm audit` is invoked anywhere in the pipeline, confirm `0 critical / 0 high / 3 moderate / 0 low` against `.paul/dean-baseline.json`; do not regenerate the baseline.
  </action>
  <verify>All five commands exit 0; npm audit unchanged from baseline.</verify>
  <done>AC-3 satisfied: release-gate verification green, manual boundary preserved.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/**` — no runtime/source changes in this phase.
- `package.json`, `package-lock.json` — version/identity already at `1.0.0`; no metadata edits here.
- `scripts/verify-release-package.sh` — Phase 58/59 verifier is stable; do not bump expectations.
- Older `docs/releases/0.*.md` notes — historical.
- `.paul/dean-baseline.json` — DEAN baseline already valid through 2026-06-11.
- Existing Phase 59 assertions in `test/release-readiness.test.ts` — extend, do not weaken.

## SCOPE LIMITS
- No actual `npm publish`, `npm publish --dry-run` against a real registry login, `git tag`, `gh release create`, or GitHub repo rename. Those belong to the user and Phase 61.
- No README rewrite (Phase 59 territory).
- No new Python helpers, runtime changes, or `pals.json` config edits.
- No new GitHub Actions workflows.

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `node --test test/release-readiness.test.ts` passes (new block + all prior assertions).
- [ ] `npm test` passes full suite.
- [ ] `npm run build` succeeds.
- [ ] `bash scripts/verify-release-package.sh` passes.
- [ ] `npm run verify:ci` passes.
- [ ] `npm pack --dry-run` produces `pi-ptc-advanced-1.0.0.tgz` with no unexpected files.
- [ ] `npm audit` baseline unchanged (`0c/0h/3m/0l`).
- [ ] All three acceptance criteria met.
- [ ] No `npm publish`, `git tag`, `gh release create`, or rename action was executed.
</verification>

<success_criteria>
- `docs/releases/PUBLISH-CHECKLIST.md` exists with all required sections and language.
- `docs/releases/1.0.0.md` and `CHANGELOG.md` link to the checklist.
- Release-readiness drift guards enforce the contract.
- Full verification green; manual publish/tag/release/rename remain explicitly user-owned.
- GitHub Flow: APPLY produces a feature branch + PR for review; merge gate is post-APPLY.
</success_criteria>

<output>
After completion, create `.paul/phases/60-1-0-release-gate/60-01-SUMMARY.md`.
</output>
