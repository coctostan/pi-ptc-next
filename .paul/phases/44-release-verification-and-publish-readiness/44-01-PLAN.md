---
phase: 44-release-verification-and-publish-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/verify-release-package.sh
  - src/python-runtime/__pycache__/runtime.cpython-314.pyc
  - docs/personal-fork-maintenance.md
  - docs/releases/0.15.0.md
autonomous: true
---

<objective>
## Goal
Close the final release gate for `pi-ptc-advanced@0.15.0` by verifying the shipped tarball contents, proving installability from the packed artifact, and removing the known packaged Python bytecode artifact without reopening completed runtime or broad documentation work.

## Purpose
Phase 42 aligned package identity and Phase 43 aligned release-facing documentation, but the project still has one concrete publish-readiness concern: `npm pack --dry-run` includes `src/python-runtime/__pycache__/runtime.cpython-314.pyc`, and the current release verification path does not yet prove a clean install from the packed tarball. This plan turns the final phase into a bounded packaging/release-validation pass rather than an open-ended release-process rewrite.

## Output
A tightened release-package verification path that proves the packed artifact contains only intended assets, a package surface that excludes the tracked Python bytecode artifact, and minimally aligned maintainer/release docs that describe the final 0.15.0 readiness checks and remaining manual publish boundaries.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/43-publish-metadata-and-documentation/43-01-SUMMARY.md

## Source Files
package.json
scripts/verify-release-package.sh
src/python-runtime/__pycache__/runtime.cpython-314.pyc
docs/personal-fork-maintenance.md
docs/releases/0.15.0.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → test infrastructure is present (`npm test` / `node --test`), but this is a bounded release-verification slice rather than a TDD-first feature phase; IRIS(150) → no actionable anti-pattern findings were detected in the likely planned packaging/release files beyond known example `TODO` strings in `README.md`, which remains out of scope; DAVE(200) → `.github/workflows/ci.yml` and `npm run verify:ci` already exist, so Phase 44 should extend the existing repo-local verification path instead of inventing publish automation; DOCS(200) → any new final verification behavior should be reflected minimally in maintainer/release docs so the active 0.15.0 baseline stays honest; RUBY(250) → planned files are small packaging/script/doc surfaces with no current debt-hotspot warning, but scope should stay bounded because `README.md` and runtime files are already large and remain out of scope.

[dispatch] pre-plan enforcement: DEAN(50) → `npm audit --json` reports 0 critical / 2 high / 3 moderate / 1 low; no critical block for plan creation, but the known audit baseline remains release context and should not be worsened.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured (`.paul/SPECIAL-FLOWS.md` not present).
</skills>

<acceptance_criteria>
## AC-1: Packaged contents exclude unintended bytecode while retaining required runtime assets
```gherkin
Given the current `npm pack --dry-run` output still includes `src/python-runtime/__pycache__/runtime.cpython-314.pyc`
When the Phase 44 package-surface cleanup is complete
Then the packed artifact excludes that bytecode artifact while still shipping the required runtime/package assets for `pi-ptc-advanced@0.15.0`
```

## AC-2: Release verification proves metadata, tarball shape, and installability from the packed artifact
```gherkin
Given `scripts/verify-release-package.sh` already checks package metadata and required tarball entries
When Phase 44 verification work is finished
Then the release verification path also proves the packed tarball can be installed in a clean temporary location and still exposes the expected package surface without unintended packaged files
```

## AC-3: Final publish-readiness guidance stays accurate and bounded
```gherkin
Given Phase 43 already aligned the release-facing documentation around `pi-ptc-advanced@0.15.0`
When this final phase lands
Then maintainer/release docs describe the strengthened final verification checks and any remaining manual publish boundaries without reopening broad README/history work or inventing automated publish/git workflows
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Remove the known packaged bytecode artifact from the release surface</name>
  <files>package.json, src/python-runtime/__pycache__/runtime.cpython-314.pyc</files>
  <action>
    Tighten the package surface so the tarball stops shipping the tracked Python bytecode artifact while preserving the intended runtime files.
    - Remove the committed `src/python-runtime/__pycache__/runtime.cpython-314.pyc` artifact from the repo/package surface.
    - Adjust the npm package include/exclude strategy only as much as needed so `npm pack --dry-run` keeps `src/python-runtime/runtime.py` (and any intentionally shipped runtime assets) while excluding bytecode/cache artifacts.
    - Keep package identity/version metadata unchanged unless a direct packaging fix requires a minimal related adjustment.
    Avoid: broad source/runtime refactors, changing package version/name again, or introducing new nonessential package-surface churn.
  </action>
  <verify>npm pack --dry-run 2>&1 | grep -E 'src/python-runtime/(runtime.py|rpc.py)|__pycache__|\.pyc' || true</verify>
  <done>AC-1 satisfied: the packed artifact excludes the bytecode/cache artifact and still includes required Python runtime assets.</done>
</task>

<task type="auto">
  <name>Task 2: Extend release-package verification to include installability proof</name>
  <files>scripts/verify-release-package.sh, package.json</files>
  <action>
    Strengthen the existing release-package verification script so it acts as the final bounded publish-readiness gate.
    - Add explicit failure checks for packaged cache/bytecode artifacts instead of relying on manual inspection.
    - Add a clean temporary install smoke check using the generated packed tarball so the release path proves the artifact can actually be installed before publish.
    - Preserve the current metadata assertions for `pi-ptc-advanced@0.15.0` and the existing required-entry checks unless the installability proof needs a minimal compatible adjustment.
    Avoid: adding `npm publish`, tag automation, branch/PR automation, or any workflow that depends on user-specific credentials/remotes.
  </action>
  <verify>bash scripts/verify-release-package.sh</verify>
  <done>AC-2 satisfied: the release verification path proves metadata, tarball shape, and clean installability from the packed artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile maintainer release guidance with the final verification gate</name>
  <files>docs/personal-fork-maintenance.md, docs/releases/0.15.0.md</files>
  <action>
    Update the maintainer-facing release docs only where needed so the active 0.15.0 baseline describes the final verification gate accurately.
    - Document the strengthened release-package verification expectations, including tarball cleanliness and installability proof.
    - Keep manual publish/git boundaries explicit.
    - Capture any remaining non-automation release steps as manual operator responsibilities rather than broadening the scope into workflow invention.
    Avoid: broad README rewrites, changelog restructuring, or historical-lineage edits already completed in Phase 43 unless the new verification path requires a minimal pointer update.
  </action>
  <verify>grep -Rni 'verify:release-package\|installab\|tarball\|manual publish\|manual release' docs/personal-fork-maintenance.md docs/releases/0.15.0.md | head -80</verify>
  <done>AC-3 satisfied: final release guidance reflects the stronger verification gate without reopening broader documentation scope.</done>
</task>

<task type="auto">
  <name>Task 4: Run the final bounded release-readiness proof set</name>
  <files>package.json, scripts/verify-release-package.sh, docs/personal-fork-maintenance.md, docs/releases/0.15.0.md</files>
  <action>
    Run the final focused verification bundle for the 0.15.0 publishable-fork baseline and capture whether any blocker remains after cleanup.
    - Run `npm run build` and `npm test` to confirm the release-surface changes did not regress the existing baseline.
    - Run `npm run verify:release-package` and `npm run verify:ci` if the strengthened checks remain fast enough for the existing repo-local path.
    - Re-check `npm pack --dry-run` output to confirm the package contents stay clean.
    - Record any remaining blocker explicitly; if no blocker remains, treat Phase 44 as the final release-readiness closure for the current manual publish model.
    Avoid: sweeping unrelated dirty working-tree changes into the verification slice.
  </action>
  <verify>npm run build && npm test && npm run verify:release-package && npm run verify:ci</verify>
  <done>AC-1, AC-2, and AC-3 are jointly proven by the final release-readiness verification bundle.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime/tool behavior under `src/**` other than removing the tracked packaged bytecode artifact from `src/python-runtime/__pycache__/runtime.cpython-314.pyc`
- README.md, CHANGELOG.md, and broad historical/lineage documentation already aligned in Phase 43 unless a direct verification pointer update becomes strictly necessary
- Version/name metadata for `pi-ptc-advanced@0.15.0` unless a direct packaging/installability fix requires a minimal correction
- GitHub-flow automation, publish automation, tagging automation, or branch/PR orchestration
- `.paul/**` artifacts other than the standard plan/state/roadmap updates created by the PAUL lifecycle

## SCOPE LIMITS
- This plan is limited to final package-surface cleanup, installability proof, and minimal maintainer/release-guidance reconciliation
- Do not reopen completed metadata renaming or broad docs realignment work from Phases 42-43
- Do not attempt to resolve the known non-critical dependency audit baseline in this phase unless it directly blocks release verification
- Keep all changes bounded around the existing dirty working tree and avoid absorbing unrelated files into the phase
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] `npm test`
- [ ] `npm run verify:release-package`
- [ ] `npm run verify:ci`
- [ ] `npm pack --dry-run` no longer shows `__pycache__` or `.pyc` entries
- [ ] Maintainer/release docs mention the final tarball/installability verification gate and keep manual publish boundaries explicit
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- The release tarball excludes `src/python-runtime/__pycache__/runtime.cpython-314.pyc`
- The repo has a repeatable proof that the packed artifact installs cleanly
- The active 0.15.0 maintainer/release docs describe the final bounded verification gate accurately
- No runtime-behavior or workflow-automation scope creep introduced
</success_criteria>

<output>
After completion, create `.paul/phases/44-release-verification-and-publish-readiness/44-01-SUMMARY.md`
</output>
