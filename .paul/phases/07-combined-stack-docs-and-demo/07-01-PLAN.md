---
phase: 07-combined-stack-docs-and-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/hashline-integration/START-HERE.md
  - docs/hashline-integration/ROADMAP.md
  - docs/hashline-integration/DEMO.md
  - docs/hashline-integration/HARNESS-EVALUATION.md
autonomous: true
---

<objective>
## Goal
Make the combined `pi-ptc-next` + `pi-hashline-readmap` stack easy to discover, configure, and reproduce by adding one canonical end-to-end search -> inspect -> edit demo, tightening the combined-stack setup guidance, and explicitly documenting whether the existing lightweight smoke proof is sufficient.

## Purpose
Phase 7 closes Milestone 3 by turning the already-correct runtime behavior into something users and extension authors can follow without guessing. This directly supports the project’s core value: `code_execution` should behave like the active Pi tools users see in chat, including trustworthy hashline-native workflows.

## Output
Updated combined-stack documentation in `README.md` and `docs/hashline-integration/`, plus two focused artifacts: a dedicated demo walkthrough and a written evaluation of whether a heavier real two-extension harness is worth adding.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/06-ptc-metadata-and-policy/06-01-SUMMARY.md

## Source Files
README.md
docs/hashline-integration/START-HERE.md
docs/hashline-integration/ROADMAP.md
test/hashline-interop-smoke.test.ts
src/index.ts
src/tool-registry.ts
</context>

<acceptance_criteria>
## AC-1: Canonical end-to-end demo exists
```gherkin
Given a user evaluating the combined `pi-ptc-next` + `pi-hashline-readmap` stack
When they follow the Phase 7 docs
Then they can see one concrete search -> inspect -> edit workflow that uses hashline-native results in Python without relying on undocumented assumptions
```

## AC-2: Setup and policy guidance is consistent across docs
```gherkin
Given a user configuring the combined stack
When they read the README and hashline integration docs
Then the installation, metadata, and policy guidance points to the same recommended setup and clearly distinguishes fallback behavior from active override behavior
```

## AC-3: Harness strategy is explicit
```gherkin
Given a maintainer deciding whether to invest in a heavier real two-extension smoke harness
When they read the final Phase 7 documentation
Then they can see the current lightweight proof, what it already validates, and a clear recommendation on whether additional harness work is needed now or deferred
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Author the canonical combined-stack demo walkthrough</name>
  <files>README.md, docs/hashline-integration/DEMO.md</files>
  <action>
    Add one primary end-to-end demo centered on the hashline-native search -> inspect -> edit flow.
    - Put the full walkthrough in `docs/hashline-integration/DEMO.md` so it can be linked from multiple places.
    - Include prerequisites, the expected combined-stack setup, a realistic `code_execution` example, and representative structured `details.ptcValue` shapes for the `grep`/`read`/`edit` flow.
    - In `README.md`, add or refine a concise summary section that links to the dedicated demo instead of duplicating too much detail.
    - Reuse the already-shipped runtime contract and smoke-proof facts; avoid inventing new runtime behavior or promising functionality that is not covered by the current code/tests.
  </action>
  <verify>Read `README.md` and `docs/hashline-integration/DEMO.md` and confirm they present a complete, internally consistent search -> inspect -> edit example tied to existing runtime behavior and proof points.</verify>
  <done>AC-1 satisfied: users have one canonical reproducible demo path for the combined stack.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile README and hashline integration setup guidance</name>
  <files>README.md, docs/hashline-integration/START-HERE.md, docs/hashline-integration/ROADMAP.md</files>
  <action>
    Tighten the combined-stack docs so setup guidance is aligned everywhere.
    - Make the recommended installation/activation story for `pi-ptc-next` + `pi-hashline-readmap` easy to find.
    - Ensure metadata and policy guidance consistently references `ptc.callable`, `ptc.policy`, and the operator-level environment filters already shipped in Phase 6.
    - Add cross-links between README, `START-HERE.md`, `ROADMAP.md`, and the new demo doc so readers can move from overview -> setup -> demo -> proof without dead ends.
    - Keep milestone notes accurate: preserve the distinction between fallback normalization defaults and richer active override payloads.
    - Avoid broad repo-wide documentation rewrites; keep edits focused on the combined-stack story for Milestone 3 closure.
  </action>
  <verify>Review the touched docs and confirm the same setup/policy story appears across all linked pages with no contradictory wording about callable metadata, policy filters, or hashline-native payload behavior.</verify>
  <done>AC-2 satisfied: setup, metadata, and policy guidance is aligned across the Phase 7 docs surface.</done>
</task>

<task type="auto">
  <name>Task 3: Document the smoke-harness recommendation and milestone-close rationale</name>
  <files>docs/hashline-integration/HARNESS-EVALUATION.md, docs/hashline-integration/ROADMAP.md, README.md</files>
  <action>
    Write down the decision framework for the optional heavier two-extension harness.
    - Create `docs/hashline-integration/HARNESS-EVALUATION.md` summarizing what `test/hashline-interop-smoke.test.ts` already proves, what a real two-extension loading harness would add, and whether that extra coverage is justified now.
    - Update `docs/hashline-integration/ROADMAP.md` to reflect the explicit recommendation from the evaluation.
    - If appropriate, add a short README pointer so maintainers know where to find the rationale.
    - Prefer a clear defer/justify recommendation unless the existing evidence genuinely supports adding immediate follow-up work; do not commit this phase to building a heavy harness unless the evaluation shows a concrete gap that cannot be documented away.
  </action>
  <verify>Read `docs/hashline-integration/HARNESS-EVALUATION.md` and the related roadmap/README references to confirm the current smoke proof, remaining gap, and recommendation are explicit and consistent.</verify>
  <done>AC-3 satisfied: maintainers can understand whether the heavyweight harness is needed now or intentionally deferred.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/` runtime implementation files unless documentation reveals a concrete factual mismatch that must be corrected in a separate plan
- Existing callable/policy semantics shipped in Phases 1-6
- `test/hashline-interop-smoke.test.ts` behavior unless a tiny clarification edit becomes strictly necessary and remains inside Phase 7 scope

## SCOPE LIMITS
- Do not reopen metadata redesign, adapter refactors, or broader Python helper ergonomics work
- Do not build a heavyweight real two-extension harness by default; this phase must first document and justify the recommendation
- Do not perform git/branch/remote automation; git actions remain user-directed
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Read the new/updated docs end-to-end for consistency and cross-link correctness
- [ ] Verify all referenced commands, env vars, and file paths exist in the repo
- [ ] Confirm the documented smoke-proof command remains `npm run build` + `node --test test/hashline-interop-smoke.test.ts`
- [ ] Confirm all acceptance criteria are met
</verification>

<success_criteria>
- All planned documentation artifacts are created or updated as listed
- The repo has one canonical combined-stack demo workflow readers can follow
- Setup/policy guidance is aligned across README and hashline integration docs
- The harness recommendation is explicit, reviewable, and scoped for Milestone 3 closure
</success_criteria>

<output>
After completion, create `.paul/phases/07-combined-stack-docs-and-demo/07-01-SUMMARY.md`
</output>
