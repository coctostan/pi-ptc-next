---
phase: 34-cross-repo-recipes-and-benchmark-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .pi/evals/ptc/recipes/graph-compact-ranking.py
  - .pi/evals/ptc/recipes/web-answer-comparison.py
  - .pi/evals/ptc/recipes/hashline-anomaly-summary.py
  - .pi/evals/ptc/recipes/codegraph-web-evidence-merge.py
  - .pi/evals/ptc/baselines/local__seeded__recipes.json
  - test/eval-cases.test.ts
  - test/benchmark-runner.test.ts
autonomous: true
---

<objective>
## Goal
Turn the seeded Phase 33 recipe-target metadata into concrete internal recipe artifacts and a deterministic recipe-suite benchmark fixture without changing the PTC runtime, helper semantics, or user-facing docs.

## Purpose
Milestone 13 now has stable machine-readable recipe targets, but Phase 34 still needs actual bounded workflow artifacts that future docs and proof can point to. Shipping concrete recipe scripts plus a deterministic recipe benchmark fixture gives Phase 35 real material to validate and document while keeping `pi-ptc-next` generic and keeping large intermediate results local to Python.

## Output
- Four concrete Python recipe artifacts for graph, web, hashline, and mixed codegraph+web workflows under `.pi/evals/ptc/recipes/`.
- One deterministic recipe-suite benchmark baseline fixture under `.pi/evals/ptc/baselines/`.
- Focused tests that lock recipe-case ↔ artifact ↔ benchmark-fixture alignment.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/33-recipe-targets-and-example-contracts/33-01-SUMMARY.md

## Source Files
.pi/evals/ptc/cases/recipe-graph-compact-ranking.json
.pi/evals/ptc/cases/recipe-web-answer-comparison.json
.pi/evals/ptc/cases/recipe-hashline-anomaly-summary.json
.pi/evals/ptc/cases/recipe-codegraph-web-evidence-merge.json
test/eval-cases.test.ts
test/benchmark-runner.test.ts
test/orchestration-ecosystem-helper.test.ts
README.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH(75) → arch_context | TODD(100) → tdd_candidates,tdd_type | IRIS(150) → review_flags | DAVE(200) → deploy_context | DOCS(200) → doc_warnings | RUBY(250) → debt_flags
[dispatch] pre-plan enforcement: DEAN(50) → dep_warnings

- **DEAN:** `npm audit --json` remains non-blocking at `0 critical / 2 high / 2 moderate / 1 low`; no DEAN baseline file is present, but the workflow only blocks on critical vulnerabilities when no baseline exists.
- **ARCH:** The repo still reads as a layered TypeScript package with benchmark/eval artifacts under `.pi/evals/ptc` and focused proof in `test/`. Planned new recipe scripts and the recipe-suite baseline stay inside that artifact layer, with no circular-dependency risk. Structural warning: `test/benchmark-runner.test.ts` is already `378` lines, so keep additions focused and avoid turning it into a broader harness.
- **TODD:** Node's built-in test runner remains the active proof path via `package.json`, and the touched benchmark/eval suites already exist. This plan can stay `execute`, but every new recipe artifact and benchmark fixture should land with narrow assertions in `test/eval-cases.test.ts` and `test/benchmark-runner.test.ts`.
- **IRIS:** No `TODO` / `FIXME` / `HACK` / `XXX` / `eval(` hits were found in the planned existing test files. Debt warning: `test/benchmark-runner.test.ts` is already above the soft line-count threshold, so prefer compact helper assertions over a mini framework.
- **DAVE:** `.github/workflows/ci.yml` already covers build/test verification through `npm run verify:ci`; no CI file changes are needed, but the final proof should keep `npm run build` and the focused test commands green.
- **DOCS:** Nearby docs exist (`README.md`, `docs/PTC-RESEARCH.md`, `docs/programmatic-tool-calling.md`), but this phase intentionally keeps user-facing recipe documentation deferred to Phase 35. Record any drift risk rather than broadening scope into docs now.
- **RUBY:** Preliminary debt review matches ARCH/IRIS: the work should stay artifact-first and avoid growing `test/benchmark-runner.test.ts` into a general benchmark harness.
- **Skipped modules:** ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, SETH, and VERA have no UI/data/API/observability/security-specific triggers in the planned file set.
</module_dispatch>

<acceptance_criteria>
## AC-1: Each seeded M4 workflow has a concrete bounded recipe artifact
```gherkin
Given the four seeded recipe cases introduced in Phase 33
When the internal recipe artifact directory is inspected
Then each workflow has a matching Python recipe that uses the shipped PTC helper surface, stays bounded, and keeps large intermediate tool results local until the final compact output
```

## AC-2: The recipe-suite benchmark fixture is deterministic and metadata-aligned
```gherkin
Given the seeded recipe cases and their additive `recipe_target` metadata
When the recipe benchmark baseline fixture is read or compared in focused tests
Then it deterministically covers the recipe subset, preserves the expected workflow/repo metadata, and remains compatible with the existing benchmark result record shape
```

## AC-3: Focused proof locks artifact, case, and benchmark alignment without widening scope
```gherkin
Given the new recipe artifacts and benchmark fixture
When the focused eval-case and benchmark tests run
Then the repo proves case-to-artifact naming, bounded-output expectations, and benchmark comparability without changing runtime semantics, helper APIs, or user-facing docs in this phase
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Author concrete Python recipe artifacts for the four seeded workflows</name>
  <files>.pi/evals/ptc/recipes/graph-compact-ranking.py, .pi/evals/ptc/recipes/web-answer-comparison.py, .pi/evals/ptc/recipes/hashline-anomaly-summary.py, .pi/evals/ptc/recipes/codegraph-web-evidence-merge.py</files>
  <action>
    Create one repo-internal Python recipe artifact per Phase 33 workflow using the seeded case metadata as the source of truth for naming and intent.
    Each script should demonstrate the shipped PTC helper surface (`ptc.batch_tool`, `ptc.reduce_tool`, `ptc.first_success`, `ptc.fit_output`, handle helpers, or live callable-tool branching where relevant) and end with a compact bounded return shape.
    Keep the scripts provider-agnostic and artifact-like: they should be concrete enough for future docs/proof to reference, but they must not depend on live adjacent repos, network availability, or new runtime features in this phase.
    Avoid changing `src/python-runtime/runtime.py`, `src/index.ts`, or README/docs while authoring these artifacts.
  </action>
  <verify>npm run build && node --test test/eval-cases.test.ts</verify>
  <done>AC-1 satisfied: every seeded recipe workflow has a concrete bounded Python artifact keyed by the existing recipe metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add a deterministic recipe-suite benchmark baseline fixture</name>
  <files>.pi/evals/ptc/baselines/local__seeded__recipes.json, test/benchmark-runner.test.ts</files>
  <action>
    Add a static benchmark baseline fixture that captures the deterministic recipe-only subset under the existing benchmark result record contract.
    Keep the fixture small and stable: cover the four recipe cases, preserve `recipe_target` metadata, and use seeded provider/model/timestamp values that make comparisons deterministic.
    Extend the focused benchmark test so it validates the fixture shape, ordering, and comparability through the current `runBenchmarkSuite`, `writeBenchmarkRun`, and `compareBenchmarkRuns` behavior rather than introducing a new benchmark runner mode.
    Avoid checking in flaky live-output captures, provider-specific token assumptions, or broader CLI/runtime redesign.
  </action>
  <verify>npm run build && node --test test/benchmark-runner.test.ts</verify>
  <done>AC-2 satisfied: the recipe benchmark fixture is deterministic, benchmark-compatible, and aligned with the seeded recipe metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Prove recipe-case, artifact, and benchmark-fixture alignment with focused tests</name>
  <files>test/eval-cases.test.ts, test/benchmark-runner.test.ts</files>
  <action>
    Expand the focused eval/benchmark suites so they assert three things explicitly:
    - every seeded recipe case maps to the expected concrete artifact by workflow name,
    - the artifact content uses bounded-output patterns that keep intermediate results local until the final return,
    - the recipe-suite benchmark baseline remains aligned with the current case inventory and result metadata.
    Keep assertions narrow and machine-checkable. Prefer file existence/content checks and benchmark-result comparisons over broader integration harnessing or doc snapshots.
    If `test/benchmark-runner.test.ts` starts to grow awkwardly, extract only the smallest local helpers needed inside the file rather than widening scope into new product code.
  </action>
  <verify>npm run build && node --test test/eval-cases.test.ts test/benchmark-runner.test.ts</verify>
  <done>AC-3 satisfied: focused proof locks the new artifacts and fixtures to the seeded recipe corpus without changing runtime/helper/doc surfaces.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `src/python-runtime/runtime.py`, `src/code-executor.ts`, `src/index.ts`, and other runtime/helper implementation files unless a blocking defect is discovered
- `README.md` and `docs/**` (Phase 35 owns user-facing recipe documentation and proof narrative updates)
- Existing benchmark runner semantics beyond additive fixture-oriented test coverage

## SCOPE LIMITS
- No live adjacent-repo integration harness, repo cloning, or network-dependent proof in this phase
- No new PTC helper APIs, no broader recipe DSL, and no schema expansion unless an artifact/fixture alignment defect makes it unavoidable
- No CI workflow changes beyond keeping existing verification green
- No omnibus test expansion into `test/index.test.ts` or `test/code-executor.test.ts`
</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] node --test test/eval-cases.test.ts
- [ ] node --test test/benchmark-runner.test.ts
- [ ] npm audit --json confirms the existing `0 critical / 2 high / 2 moderate / 1 low` baseline does not worsen
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All focused verification checks pass
- Four concrete recipe artifacts exist under `.pi/evals/ptc/recipes/`
- The recipe-suite benchmark baseline exists under `.pi/evals/ptc/baselines/` and matches the current deterministic contract
- The phase remains additive: no runtime/helper/doc surface expansion beyond what is needed to lock the new artifacts and fixtures
</success_criteria>

<output>
After completion, create `.paul/phases/34-cross-repo-recipes-and-benchmark-fixtures/34-01-SUMMARY.md`
</output>
