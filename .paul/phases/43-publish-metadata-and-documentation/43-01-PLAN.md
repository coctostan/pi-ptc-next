---
phase: 43-publish-metadata-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - docs/personal-fork-maintenance.md
  - docs/releases/0.15.0.md
autonomous: true
---

<objective>
## Goal
Align the user-facing publish/install/docs surface with the already-renamed package baseline `pi-ptc-advanced@0.15.0`, including README install guidance, maintainer/release documentation, changelog framing, and explicit fork-lineage acknowledgment.

## Purpose
Phase 42 changed the actual package metadata, but the publish-facing documentation still advertises the old `pi-ptc-next` / `0.8.0` baseline. This plan closes that documentation gap so the repo presents a coherent publish target without reopening runtime behavior, package metadata, or release-automation scope.

## Output
Updated README/install guidance, refreshed maintainer and release-facing documentation for the `0.15.0` publish baseline, a new `docs/releases/0.15.0.md` release note, and aligned changelog entries that preserve fork lineage and manual-release boundaries.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/42-rename-and-package-identity/42-01-SUMMARY.md

## Source Files
README.md
CHANGELOG.md
docs/personal-fork-maintenance.md
docs/releases/0.15.0.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → test infrastructure exists (`node --test` via `npm test`), but the planned slice is documentation-only and does not warrant a TDD plan type; IRIS(150) → no actionable anti-pattern findings in the planned Markdown/doc surfaces; DAVE(200) → `.github/workflows/ci.yml` and repo-local verification commands already exist, so docs should describe the established manual publish boundary rather than invent automation; DOCS(200) → confirmed active drift in `README.md`, `CHANGELOG.md`, `docs/personal-fork-maintenance.md`, and `docs/releases/0.8.0.md` against the new `pi-ptc-advanced@0.15.0` package baseline; RUBY(250) → planned files are documentation surfaces with no debt-hotspot concerns beyond keeping scope bounded.

[dispatch] pre-plan enforcement: DEAN(50) → `npm audit --json` reports 0 critical / 2 high / 3 moderate / 1 low; no critical block for plan creation, but the known audit baseline remains carry-forward release context.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured (`.paul/SPECIAL-FLOWS.md` not present).
</skills>

<acceptance_criteria>
## AC-1: README and install guidance match the publish target and lineage
```gherkin
Given Phase 42 already renamed the actual npm package surface to `pi-ptc-advanced@0.15.0`
When the Phase 43 documentation work is complete
Then `README.md` describes the current publish/install target, links to the correct release docs, and clearly acknowledges the lineage from the original upstream and the prior `pi-ptc-next` fork
```

## AC-2: Release-facing docs and changelog describe the 0.15.0 baseline
```gherkin
Given maintainers and users rely on repo-local release-facing docs to understand what ships
When this phase is finished
Then `CHANGELOG.md`, `docs/personal-fork-maintenance.md`, and a `docs/releases/0.15.0.md` note all describe the `pi-ptc-advanced@0.15.0` baseline without leaving the old `0.8.0` release framing as the active target
```

## AC-3: Documentation stays honest about scope and release workflow boundaries
```gherkin
Given this phase is documentation-only follow-up to the package rename
When the updates land
Then the docs do not imply new runtime/package behavior or automated publish/git flows, and any remaining publish-readiness concerns stay explicitly deferred to Phase 44
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Rewrite README publish/install messaging around the new package baseline</name>
  <files>README.md</files>
  <action>
    Update the README sections that still advertise `pi-ptc-next` / `0.8.0` as the active public release surface.
    - Replace stale install and release-baseline language with wording that reflects `pi-ptc-advanced@0.15.0` as the current publish target.
    - Add explicit fork-lineage acknowledgment that the current package/repo history passes through the earlier `pi-ptc-next` fork while still crediting `cegersdoerfer/pi-ptc` as the original upstream.
    - Update release-doc links so the README points at the active `0.15.0` release note rather than the older `0.8.0` baseline as current guidance.
    Avoid: changing runtime behavior claims, expanding into a broad README rewrite, or implying package-registry publication automation that does not exist.
  </action>
  <verify>grep -n 'pi-ptc-advanced\|0.15.0\|pi-ptc-next\|cegersdoerfer/pi-ptc' README.md | head -40</verify>
  <done>AC-1 satisfied: README install/release messaging and lineage acknowledgment reflect the current publish target.</done>
</task>

<task type="auto">
  <name>Task 2: Align changelog and maintainer/release docs to the 0.15.0 publish baseline</name>
  <files>CHANGELOG.md, docs/personal-fork-maintenance.md, docs/releases/0.15.0.md</files>
  <action>
    Update the release-facing documentation set so the active documented baseline matches the package metadata.
    - Add a new `docs/releases/0.15.0.md` note that explains what the 0.15.0 publishable-fork baseline is for, what changed, and what remains deferred.
    - Update `docs/personal-fork-maintenance.md` so release-package verification and maintainer guidance refer to the `0.15.0` target and link to the new release note.
    - Update `CHANGELOG.md` with a 0.15.0 entry that captures the package rename/documentation alignment without erasing the older `0.8.0` history.
    Avoid: deleting historical `0.8.0` release context unless a link must move from “current” to “historical”, and avoid broad release-process invention beyond what the repo actually supports.
  </action>
  <verify>grep -Rni '0\.15\.0\|pi-ptc-advanced\|0\.8\.0' CHANGELOG.md docs/personal-fork-maintenance.md docs/releases/0.15.0.md | head -80</verify>
  <done>AC-2 satisfied: release-facing docs and changelog now present 0.15.0 as the active documented baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify docs/package consistency without reopening implementation scope</name>
  <files>README.md, CHANGELOG.md, docs/personal-fork-maintenance.md, docs/releases/0.15.0.md</files>
  <action>
    Run bounded verification that the documentation surface now matches the package baseline and still respects release-workflow constraints.
    - Re-run `npm run verify:release-package` so the package surface remains valid after the documentation updates.
    - Grep the updated docs for stale active-target references and confirm any remaining mentions of `pi-ptc-next` or `0.8.0` are clearly historical/lineage context rather than current install guidance.
    - Capture any still-open publish-readiness concerns, such as tarball cleanup or installability checks, as Phase 44 follow-up rather than broadening this phase into final release verification.
    Avoid: touching `package.json`, `package-lock.json`, `scripts/verify-release-package.sh`, or runtime/test source unless a direct verification failure makes a minimal correction unavoidable.
  </action>
  <verify>npm run verify:release-package && grep -Rni 'pi-ptc-next\|0\.8\.0\|pi-ptc-advanced\|0\.15\.0' README.md CHANGELOG.md docs 2>/dev/null | head -120</verify>
  <done>AC-3 satisfied: docs/package consistency is rechecked and remaining release-readiness work stays explicitly deferred to Phase 44.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime, tool-registry, RPC, and Python execution behavior under `src/**`
- Package metadata and release-package verification logic already aligned in Phase 42 (`package.json`, `package-lock.json`, `scripts/verify-release-package.sh`) unless documentation-only verification exposes a direct blocker
- GitHub workflow semantics beyond documentation wording that describes the current manual publish boundary
- `.paul/**` artifacts other than the standard plan/state/roadmap updates created by the PAUL lifecycle

## SCOPE LIMITS
- This plan covers publish metadata/documentation alignment only
- Do not add npm publish automation, tagging automation, PR automation, or broader git workflow changes
- Do not treat this plan as the final release-readiness gate; installability/tarball cleanup/final package review belong to Phase 44
- Keep older `0.8.0` references only where they remain useful as historical context, not as the active release target
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run verify:release-package`
- [ ] `grep -n 'pi-ptc-advanced\|0.15.0\|pi-ptc-next\|cegersdoerfer/pi-ptc' README.md | head -40`
- [ ] `grep -Rni '0\.15\.0\|pi-ptc-advanced\|0\.8\.0' CHANGELOG.md docs/personal-fork-maintenance.md docs/releases/0.15.0.md | head -80`
- [ ] Remaining publish-readiness work is explicitly deferred to Phase 44
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- Active docs/install guidance consistently point to `pi-ptc-advanced@0.15.0`
- Fork lineage is explicit and accurate in the README/release-facing docs
- Release documentation remains honest about manual publish/git boundaries
- No runtime or package-behavior scope creep introduced
</success_criteria>

<output>
After completion, create `.paul/phases/43-publish-metadata-and-documentation/43-01-SUMMARY.md`
</output>
