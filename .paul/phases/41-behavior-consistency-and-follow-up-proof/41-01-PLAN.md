---
phase: 41-behavior-consistency-and-follow-up-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python-runtime/runtime.py
  - src/utils.ts
  - src/index.ts
  - test/orchestration-helper.test.ts
  - test/live-audit-stress.test.ts
  - test/live-audit-pipeline.test.ts
  - test/utils.test.ts
  - test/index.test.ts
  - test/orchestration-ecosystem-contract.test.ts
  - README.md
autonomous: true
---

<objective>
## Goal
Resolve the remaining behavior-consistency follow-ups from Milestone 14 by adding bounded partial-result support to `ptc.batch_tool(...)`, aligning empty-input behavior for `batch_tool([])`, and tightening output truncation framing so executor output respects `maxOutputChars` directly.

## Purpose
Phase 41 closes Milestone 15 by finishing the open behavior decisions left after the P0 file-discovery and syntax-error hardening slices. The helper surface should stay predictable for models while preserving bounded behavior and compatibility for existing callers.

## Output
- `src/python-runtime/runtime.py` updates for `batch_tool` partial mode + empty-input alignment
- `src/utils.ts` output truncation framing update
- `src/index.ts` code_execution helper guidance update
- Focused test updates and proof refresh in orchestration/live-audit contract files
- README helper contract update for the new `batch_tool` call shape
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/38-composition-patterns-and-audit-scorecard/FINAL-AUDIT.md
.paul/phases/39-p0-file-discovery-repair/39-01-SUMMARY.md
.paul/phases/40-error-handling-hardening/40-01-SUMMARY.md

## Source Files
src/python-runtime/runtime.py
src/utils.ts
src/index.ts
test/orchestration-helper.test.ts
test/live-audit-stress.test.ts
test/live-audit-pipeline.test.ts
test/utils.test.ts
test/index.test.ts
test/orchestration-ecosystem-contract.test.ts
README.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan enforcement: DEAN(50) → `npm audit --json` reports 0 critical / 2 high / 3 moderate / 1 low (`fast-xml-parser`, `undici` high); no critical block, planning may proceed.

[dispatch] pre-plan advisory: TODD(100) → Node built-in runner workflow confirmed (`npm test` uses `node --test`) with broad existing `*.test.ts` coverage, including direct orchestration/live-audit targets. | IRIS(150) → no TODO/FIXME/HACK/XXX markers in planned source/test files; README contains literal `TODO`/`FIXME` sample strings in an example block only. | DAVE(200) → `.github/workflows/ci.yml` exists and executes `npm ci` + `npm run verify:ci`; explicit standalone lint step remains advisory/missing. | DOCS(200) → repo docs catalog present (`README.md` + `docs/*`); this plan includes README contract updates to reduce drift for helper behavior changes. | RUBY(250) → debt hotspots noted (`runtime.py` 743 lines, `README.md` 853 lines, `test/index.test.ts` 1220 lines); keep edits surgical and bounded.
</module_dispatch>

<acceptance_criteria>
## AC-1: `ptc.batch_tool(...)` supports bounded partial-result collection without breaking default behavior
```gherkin
Given a batch with mixed successful and failing nested tool calls
When `ptc.batch_tool(calls, on_error='collect')` is used
Then it returns a bounded `batch_partial` envelope with ordered per-call success/error entries and summary counts
```

## AC-2: Empty-input and output-framing behavior is consistent and predictable
```gherkin
Given an empty call list and a strict output limit
When `ptc.batch_tool([])` and executor truncation are exercised
Then `batch_tool([])` returns `[]` and final output remains within `maxOutputChars` while still providing truncation context when space allows
```

## AC-3: Follow-up proof and user-facing helper contracts are aligned with shipped behavior
```gherkin
Given the Phase 41 behavior changes are implemented
When focused orchestration/live-audit/contract tests and full suite verification run
Then tests pass and helper guidance in `src/index.ts` and `README.md` reflects the updated `batch_tool` contract
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Implement `batch_tool` partial-result mode and empty-input alignment in runtime helpers</name>
  <files>src/python-runtime/runtime.py</files>
  <action>
    Update orchestration call normalization and `ptc.batch_tool(...)` behavior:
    - allow empty call lists for `batch_tool` while keeping non-empty enforcement for helpers that still require candidates (for example `first_success`)
    - add `on_error` mode normalization with default `raise` and opt-in `collect`
    - preserve current fail-fast behavior for default mode
    - implement `collect` mode to return ordered per-call result/error entries plus bounded stats

    Avoid broad runtime refactors outside orchestration helper behavior.
  </action>
  <verify>npm run build && node --test test/orchestration-helper.test.ts test/live-audit-stress.test.ts</verify>
  <done>AC-1 satisfied: default + collect modes are both covered and pass in focused runtime tests.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten output truncation framing and align code_execution helper guidance</name>
  <files>src/utils.ts, src/index.ts, test/live-audit-pipeline.test.ts, test/utils.test.ts, test/index.test.ts</files>
  <action>
    Update truncation handling so `truncateOutput(...)` respects the configured character cap while preserving a truncation notice when budget allows.
    Align code_execution helper description text and associated tests with the updated `batch_tool` signature and collect-mode note.

    Keep the changes bounded to truncation framing + helper guidance assertions; do not alter unrelated routing/recovery logic.
  </action>
  <verify>npm run build && node --test test/live-audit-pipeline.test.ts test/utils.test.ts test/index.test.ts</verify>
  <done>AC-2 and AC-3 satisfied: truncation and helper-description contracts are validated in focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh follow-up proof + README contract coverage and run full verification</name>
  <files>test/orchestration-ecosystem-contract.test.ts, README.md, test/live-audit-stress.test.ts, test/orchestration-helper.test.ts</files>
  <action>
    Update the ecosystem contract proof and README orchestration helper section to reflect the new `batch_tool` call shape (`on_error` support and collect-mode behavior).
    Ensure stress/orchestration proof remains deterministic for mixed success/failure handling and empty-input behavior.
    Finish with full suite verification to close the phase with durable evidence.
  </action>
  <verify>node --test test/orchestration-ecosystem-contract.test.ts && npm test</verify>
  <done>AC-3 satisfied: documentation/contracts/tests are aligned and the full suite stays green.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Unrelated runtime surfaces outside the targeted orchestration/truncation behavior (`read_tree`, handle helpers, recovery classifier, rpc protocol)
- Release/version/package metadata and milestone history artifacts outside this phase
- Broad README restructuring beyond helper-contract alignment for changed behavior

## SCOPE LIMITS
- Keep this plan focused on Phase 41 follow-ups: batch partial behavior, empty-input consistency, truncation framing expectations, and proof refresh
- Do not introduce a new orchestration DSL or additional helper APIs beyond the bounded `batch_tool` mode parameter
- If additional future helper ergonomics emerge, capture them as deferred follow-up instead of expanding this plan
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `node --test test/orchestration-helper.test.ts test/live-audit-stress.test.ts` passes
- [ ] `node --test test/live-audit-pipeline.test.ts test/utils.test.ts test/index.test.ts` passes
- [ ] `node --test test/orchestration-ecosystem-contract.test.ts` passes
- [ ] `npm test` passes with no regressions
- [ ] Changed implementation/docs files remain within this plan's bounded scope
</verification>

<success_criteria>
- `ptc.batch_tool` supports explicit bounded partial-result collection while preserving default fail-fast semantics
- Empty `batch_tool([])` behavior is aligned with other list-based helpers (`[]`)
- Truncation framing no longer overshoots configured output caps
- Runtime guidance and README contract text match shipped helper behavior
- Focused + full-suite verification evidence is green
</success_criteria>

<output>
After completion, create `.paul/phases/41-behavior-consistency-and-follow-up-proof/41-01-SUMMARY.md`
</output>
