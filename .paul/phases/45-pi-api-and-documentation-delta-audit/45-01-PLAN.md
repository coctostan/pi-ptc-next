---
phase: 45-pi-api-and-documentation-delta-audit
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - .paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md
autonomous: false
---

<objective>
## Goal
Produce a compatibility delta audit comparing `pi-ptc-advanced` against both the installed local `@earendil-works/pi-coding-agent@0.74.0` package and the latest published Pi package/docs/source, with explicit findings for extension API usage, tool metadata, callable-tool execution, and system-prompt/tool-guidance integration.

## Purpose
Milestone 17 should make sure `pi-ptc-advanced` remains optimally compatible with current Pi extension APIs before Phase 46 applies runtime changes or Phase 47 changes prompt guidance. This plan is intentionally audit-only so remediation scope is evidence-based instead of speculative.

## Output
Create `.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md` containing source evidence, compatibility matrix, prompt-integration findings, risk-ranked recommendations, and a proposed Phase 46/47 handoff.
</objective>

<context>
## Project Context
- `.paul/STATE.md`: Milestone 17 is exploratory/high-collaboration; Phase 45 must produce the compatibility delta audit before runtime or prompt-integration changes.
- `.paul/ROADMAP.md`: Phase 45 focus is local + latest Pi API/docs/source comparison for API drift, deprecated assumptions, new extension capabilities, and prompt/tool-description convention changes.
- `.paul/dean-baseline.json`: User-approved DEAN override baseline for pre-existing audit findings; do not turn this phase into dependency remediation.

## Local Pi Documentation Already Sampled During Planning
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/README.md`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/docs/extensions.md`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/docs/sdk.md`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/docs/prompt-templates.md`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/docs/tui.md`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/examples/extensions/prompt-customizer.ts`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/examples/extensions/dynamic-tools.ts`
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/examples/extensions/tool-override.ts`

## Source Files To Inspect During APPLY
- `package.json`
- `src/index.ts`
- `src/tool-registry.ts`
- `src/contracts/tool-types.ts`
- `src/tools/python-tool-contract.ts`
- `src/custom-tool-manager.ts`
- `README.md`
- Relevant focused tests only if needed for proving current assumptions: `test/index.test.ts`, `test/tool-registry.test.ts`, `test/python-tool-contract.test.ts`, `test/hashline-default-exposure.test.ts`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
- [dispatch] pre-plan advisory: TODD detected Node test infrastructure (`npm test` runs `npm run build && node --test test/*.test.ts`; test files are present). Because Phase 45 is research/audit-only, do not convert this plan to TDD; require verification commands and audit content checks instead.
- [dispatch] pre-plan advisory: IRIS found no TODO/FIXME/HACK/XXX signatures in the planned Phase 45 directory; planned source changes are limited to a PALS audit artifact.
- [dispatch] pre-plan advisory: DAVE detected `.github/workflows/ci.yml`; no CI file is planned for modification.
- [dispatch] pre-plan advisory: DOCS planned artifact is itself documentation under `.paul/phases/45...`; no product docs should be edited in this audit-only phase.
- [dispatch] CODI: skipped — no extractable symbols in phase scope. Planned APPLY changes only create an audit document; source identifiers are inspection targets, not behavior-change seeds.
- [dispatch] pre-plan advisory: RUBY notes debt context from STATE: `src/python-runtime/runtime.py` and README/test hotspots are deferred concerns; this audit must not casually grow source/docs hotspots.
- [dispatch] pre-plan enforcement: DEAN BLOCK overridden by user. `npm audit --json` reported 1 critical, 4 high, 5 moderate vulnerabilities; user selected override/baseline. Baseline written to `.paul/dean-baseline.json` with 30-day staleness window.
</module_dispatch>

<skills>
No SPECIAL-FLOWS.md - specialized flows omitted.
</skills>

<acceptance_criteria>
## AC-1: Evidence-Based Pi Version Baseline
```gherkin
Given the repository and installed Pi package are available
When Phase 45 APPLY completes
Then the audit records the local installed Pi version, latest published Pi version/dist-tag, source/doc locations consulted, and whether local equals latest
```

## AC-2: Extension API Compatibility Matrix
```gherkin
Given current `pi-ptc-advanced` extension code and latest Pi extension docs/source
When Phase 45 APPLY completes
Then the audit identifies compatible, risky, deprecated, or upgrade-candidate usage for tool registration, active-tool discovery, tool metadata, rendering, event hooks, and runtime lifecycle assumptions
```

## AC-3: System Prompt and Tool Guidance Findings
```gherkin
Given latest Pi guidance for `promptSnippet`, `promptGuidelines`, `before_agent_start`, `systemPromptOptions`, and context/tool snippets
When Phase 45 APPLY completes
Then the audit names concrete prompt-integration opportunities or confirms no change is needed, with Phase 47-ready recommendations
```

## AC-4: Bounded Remediation Handoff
```gherkin
Given the compatibility audit is complete
When the audit is reviewed
Then recommendations are prioritized into Phase 46 runtime-alignment candidates, Phase 47 prompt-guidance candidates, Phase 48 proof/release candidates, and explicit deferrals
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Build local/latest Pi evidence baseline</name>
  <files>.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md</files>
  <action>
    Create the audit document with an evidence section. Record:
    - local package identity/version from `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/package.json`
    - project package identity/version and Pi package metadata from `package.json`
    - latest published version/dist-tag from `npm view @earendil-works/pi-coding-agent version dist-tags --json`
    - local docs and examples consulted, including extensions, SDK, prompt templates, TUI, prompt-customizer, dynamic-tools, and tool-override
    - remote/latest docs/source consulted where network access allows; if local version equals latest published version, state that local installed docs are the executable latest baseline and still record the npm evidence
    Avoid: changing runtime source, package dependencies, or public docs in this audit phase.
  </action>
  <verify>`test -f .paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md && grep -n "Local Pi baseline\|Latest published baseline\|Evidence" .paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md</verify>
  <done>AC-1 satisfied: the audit has reproducible local/latest Pi version and source evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Audit extension API and runtime compatibility</name>
  <files>.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md</files>
  <action>
    Inspect current code against latest Pi extension guidance and write a compatibility matrix. Minimum rows:
    - package/import namespace (`@mariozechner/*` currently used by project source vs latest docs using `@earendil-works/*`)
    - `pi.registerTool()` definition shape, including `label`, `description`, `parameters`, `prepareArguments`, `execute`, `renderCall`, `renderResult`, and `terminate`
    - prompt metadata: `promptSnippet` and `promptGuidelines` versus current `description`-only `code_execution` guidance
    - active tool discovery: `pi.getActiveTools()`, `pi.getAllTools()`, `sourceInfo`, and current assumptions in `ToolRegistry`
    - built-in override behavior and whether current hashline/native interop bridge can be simplified by newer APIs
    - `before_agent_start`, `context`, `before_provider_request`, and current automatic recovery/prompt behavior
    - TUI render contract: `Component.render(width)` line-width rule, `Text` usage, `renderShell`, and render fallback behavior
    For each row, mark status as `compatible`, `upgrade-candidate`, `risk`, or `defer`, and include exact file/doc evidence.
  </action>
  <verify>`grep -n "Compatibility Matrix\|promptSnippet\|systemPromptOptions\|getAllTools\|@mariozechner" .paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md</verify>
  <done>AC-2 and AC-3 satisfied: API and prompt-integration findings are explicit and evidence-linked.</done>
</task>

<task type="auto">
  <name>Task 3: Produce prioritized remediation and proof handoff</name>
  <files>.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md</files>
  <action>
    Add a prioritized handoff section that separates:
    - Phase 46 runtime compatibility candidates
    - Phase 47 system-prompt/tool-guidance candidates
    - Phase 48 proof/release-readiness candidates
    - explicit deferrals and risks
    Include a small decision table with recommendation, rationale, expected files, and verification strategy. Keep recommendations bounded; do not prescribe broad rewrites unless the evidence shows an actual compatibility problem.
  </action>
  <verify>`grep -n "Phase 46 Runtime\|Phase 47 Prompt\|Phase 48 Proof\|Deferrals" .paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md</verify>
  <done>AC-4 satisfied: next-phase work is actionable without expanding this audit into implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A compatibility delta audit document for Phase 45.</what-built>
  <how-to-verify>
    1. Read `.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md`.
    2. Confirm the evidence sources include both installed local Pi and latest published Pi baseline.
    3. Confirm findings distinguish runtime compatibility candidates from prompt-guidance candidates.
    4. Confirm no runtime/source implementation was changed during the audit phase.
  </how-to-verify>
  <resume-signal>Type "approved" to continue to UNIFY, or describe audit changes needed first</resume-signal>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime/source implementation files during Phase 45 APPLY (`src/**/*.ts`, `src/python-runtime/**/*.py`)
- Public package metadata/dependency versions (`package.json`, `package-lock.json`) except reading them for evidence
- Release scripts and CI configuration
- Installed Pi package files under `/opt/homebrew/lib/node_modules/...`

## SCOPE LIMITS
- This is an audit/research plan only; Phase 46 handles runtime implementation, Phase 47 handles prompt/tool-guidance changes, and Phase 48 handles release proof.
- Network access may be used for `npm view` and remote docs/source inspection; record failures as evidence gaps instead of blocking if local installed Pi equals latest published npm version.
- The DEAN audit baseline is acknowledged for planning only; do not remediate dependencies in this plan.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-PI-COMPAT-AUDIT.md` exists.
- [ ] Audit includes local installed Pi version and latest published Pi version/dist-tags.
- [ ] Audit includes a compatibility matrix with statuses and evidence links/paths.
- [ ] Audit includes Phase 46/47/48 handoff recommendations and deferrals.
- [ ] `npm run build` passes to prove the repository remains buildable after audit-only changes.
- [ ] `git diff --name-status` shows only the planned audit artifact plus lifecycle artifacts expected from APPLY/UNIFY.
</verification>

<success_criteria>
- Audit artifact is complete, evidence-based, and bounded to Phase 45.
- No runtime implementation, package dependency, or public docs changes are made during APPLY.
- Findings are prioritized enough to plan Phase 46 and Phase 47 without rediscovering the same evidence.
- Human verification checkpoint is satisfied before UNIFY.
</success_criteria>

<output>
After completion, create `.paul/phases/45-pi-api-and-documentation-delta-audit/45-01-SUMMARY.md` with plan-vs-actual results, audit findings summary, verification commands, module outcomes, and next-phase recommendation.
</output>
