# PAUL Handoff

**Date:** 2026-03-19
**Status:** paused — milestone boundary, clean state

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** `pi-ptc-next` — a provider-agnostic `code_execution` tool for pi that lets models write Python, call pi tools from Python over a JSON RPC bridge, and return only the compact final output.

**Core value:** `code_execution` should use the same active Pi tool implementations the user sees in chat, so nested read/grep/edit calls get trustworthy hashline-native behavior without split-brain tool semantics.

---

## Current State

**Version:** package.json=0.1.1 (PALS docs=0.5.0 — version bump deferred)
**Milestone:** Awaiting next milestone (Milestone 6 just completed)
**Phase:** None active
**Plan:** None

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Milestone complete - ready for next]
```

---

## What Was Done (This Session)

### Milestone 3 — Completed (was pending from prior session)
- Closed out Python Ergonomics and Metadata milestone

### Milestone 4 — Cross-Extension Tool Execution Bridge (new)
- Implemented EventBus bridge: hashline emits tool executors on `"hashline:tool-executors"`, PTC subscribes and overlays in `buildToolMap()`
- `globalThis.__hashlineToolExecutors` dual-path handles load-order (hashline fires before PTC subscribes)
- `read()` inside `code_execution` now returns structured `ReadResult` dicts with `line`, `hash`, `anchor` fields
- `grep()` returns structured `GrepResult` with anchor fields
- Split-brain eliminated — 3 phases, ~35 source lines, 73/73 tests

### Milestone 5 — Python Helper Normalization (found via live audit)
- Bridge caused `ptc.read_text()`, `ptc.read_many()`, `ptc.read_tree()` to return dicts instead of strings
- Added `_extract_text()` in `runtime.py` to normalize structured results back to strings
- `read()` stays `Union[str, ReadResult]` — helpers normalize, direct calls don't
- 2 phases, ~10 lines

### Milestone 6 — Review Findings Remediation
- Holistic pressure test identified 6 findings (1 HIGH, 3 MEDIUM, 2 LOW)
- **E9 (HIGH):** RPC `asyncio.StreamReader` buffer 64KB → 4MB. README.md: 270s hang → 7ms.
- **E8 (MEDIUM):** grep record paths normalized to workspace-relative via `_relativize_path()` wrapper.
- **D1+D4 (MEDIUM):** 4 operator-facing rules removed from model description (~400 chars saved).
- **D2 (MEDIUM):** "Prefer these for string content" + "Use read() for structured anchored data" guidance added.
- **D3 (LOW):** String content helpers documented inline.
- 6/6 findings resolved, 0 deferred. 2 phases, 73/73 tests.

### E2E Test Harness
- Built `test/e2e-agent-harness.sh` — 35-scenario end-to-end test suite exercising the real Python RPC bridge
- 35/35 pass

---

## What's In Progress

- Nothing in progress. Clean milestone boundary.

---

## What's Next

**Immediate:** Define the next milestone. Candidate: **Release and Packaging** — version bump (0.1.1 → 0.5.0), CHANGELOG.md, npm publish readiness, CI pipeline.

**After that:** No further candidates defined. The core interop story across 6 milestones is complete.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview — 6 milestones collapsed |
| `.paul/MILESTONES.md` | Full accomplishment log |
| `src/tool-registry.ts` | Core bridge — extensionExecutors + EventBus subscriber |
| `src/python-runtime/runtime.py` | _extract_text + grep normalization |
| `src/python-runtime/rpc.py` | RPC transport with 4MB buffer |
| `src/index.ts` | code_execution tool registration + model-facing description |
| `test/e2e-agent-harness.sh` | 35-scenario end-to-end test harness |

---

## Git State

**Branch:** `feat/hashline-native-interop`
**Uncommitted changes:** 19 modified files + untracked `.paul/` directory
**All changes span Milestones 3–6** — no commits have been made (git automation is user-directed, remote owned by upstream)

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Check loop position — currently at milestone boundary
3. Run `/paul:resume` or `/paul:status`
4. Next action: `/paul:discuss-milestone` or `/paul:milestone` for Release and Packaging

---

*Handoff created: 2026-03-19*
