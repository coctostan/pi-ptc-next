# pi-ptc-advanced 1.0.0

`pi-ptc-advanced@1.0.0` is the public identity baseline for this package. It updates the active package and release-surface metadata from the earlier fork-first `pi-ptc-next` framing to the intended public `pi-ptc-advanced` package and `coctostan/pi-ptc-advanced` repository identity.

## Identity baseline

- Package name: `pi-ptc-advanced`
- Version: `1.0.0`
- Intended public repository identity: `coctostan/pi-ptc-advanced`
- Prior fork/repo lineage remains credited in README and historical release material.

## Verification surface

This baseline is guarded by:

- `node --test test/release-readiness.test.ts`
- `npm run build`
- `bash scripts/verify-release-package.sh`
- `npm run verify:ci`

The release-package verifier checks package metadata, the dry-run tarball surface, and clean install proof for `pi-ptc-advanced@1.0.0`.

## Manual boundaries

This note does not claim that publishing has happened. Actual `npm publish`, npm dist-tags, git tags, GitHub releases, and the GitHub repository rename are manual/user-owned operations and remain outside automated APPLY work.
